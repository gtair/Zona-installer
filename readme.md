# how to use
1. Extract the folder somewhere
> [!WARNING]
> do NOT extract the contents of the folders. Random files inside the `installer` folder may get deleted
2. run `Start-Installer.bat`
3. Follow instructions